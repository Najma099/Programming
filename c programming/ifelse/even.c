#include<stdio.h>
int main()
{
    int n;
    printf("Enter the vlue of n:");
    scanf("%d",&n);
    if(n%2==0)
    printf("the number is even");
    else 
    printf("The number is odd");
    return 0;
}
