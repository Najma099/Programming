#include<stdio.h>
int main()
{
    int radius;
    printf("enter the radius:");
    scanf("%d",&radius);
    float pi=3.14;
    float area;
    printf("area is %f:",radius*radius*pi);
    return 0;

}