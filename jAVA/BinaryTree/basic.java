class Node {
    int data;
    Node left;
    Node right;
    Node(int data) {
        this.data = data;
    }
}
// public class Tree {

// }
public class basic {
    public static void main(String[] args) {
        // Node a = new Node(1);
        // Node b = new Node(41);
        // Node c = new Node (3);
        // Node d = new Node(2);
        // Node e = new Node(2);
        // Node f = new Node(2);
    }
}